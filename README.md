# Проект по использованию selenium, python u pyttest

## How to start the project

## Commnads of git

1. Иницилизация git
```
git init
```
2. create a commit
```
git status.
git add . .
git status.
git commit -m "Have a massege"
```
3. list of commits
```
git log -- oneline -N
```
4. Создание ssh соединения
```
ssh-keygen -t ed25519 -C "@".
ls -la.
ssh -T git@name.ru(com)
clip < id_ed.
Вставить в github.
```
5. Config Настройка
```
Host name.ru(com)
	HostName github.com
	User name
	IdentityFile ~/.ssh/namefile
	IdentitiesOnly=yes